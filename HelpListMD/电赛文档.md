## ToDoList

- 学习verilog
- vivado和 PL编写 'AD-DA'驱动
- vitis和 PS 端将 PL 采集到的数据传输到千兆网口

## PL端驱动和PS接收

- 使用DMA和PS端通信
- 使用FIFO
- 每一次发送4096个数据，当PS端完全接收时，给出信号，PL再次发送4096个数据。
- PS端使用双Buffer，DMA写入bufferA的时候，BufferB向千兆网口传输数据。
- 千兆网口使用UDP，加包头
- 多添加异常标识方便调试，比如数据过多，数据过少

## 任务流程

1.用信号发生器向PL端发送数据，和PS端的通信先跑通，FIFO结构实现，包头，数据回复帧确定。
2.PL端适应新的ADDA板。
3.PS向千兆网口发送数据。
